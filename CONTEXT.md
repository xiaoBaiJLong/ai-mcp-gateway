# MCP 网关领域

本领域描述一个企业内部使用的 MCP 网关。它将业务能力统一暴露为受治理的 MCP 工具，使 Agent 无需直接了解各业务服务或远程 MCP 服务的接入细节。

## 语言

**MCP 网关**：
Agent 发现和调用工具的统一入口，同时负责工具转换、聚合、权限控制和调用治理。
_避免_：AI 网关、工具代理

**Agent**：
通过 MCP 网关发现并调用获授权工具的 AI 应用。
_避免_：用户、租户、MCP 服务

**Agent 凭证**：
用于识别和认证一个 Agent 的秘密凭据；第一阶段采用每个 Agent 独立的 API Key。
_避免_：用户密码、业务服务凭证

**最终用户**：
通过 Agent 发起业务意图、并受到业务服务数据权限约束的人；最终用户与执行调用的 Agent 是两个不同的权限主体，网关只透传其身份凭据而不验证。
_避免_：Agent、管理员、API Key 持有者

**业务服务**：
向 MCP 网关提供业务 HTTP API 的上游系统。
_避免_：MCP 服务、工具服务

**远程 MCP 服务**：
已经实现 MCP 协议、由 MCP 网关代理和治理的上游服务。
_避免_：业务服务、内置工具

**工具**：
Agent 可通过 MCP 调用的一项业务能力，其来源可以是业务服务的 HTTP API，也可以是远程 MCP 服务暴露的工具。
_避免_：接口、插件、函数

**工具定义**：
MCP 网关中描述一个工具及其输入、输出和上游能力关系的可发布定义。
_避免_：接口配置、工具代码

**工具目录**：
MCP 网关根据 Agent 权限提供的可发现工具集合。
_避免_：全量工具列表、插件市场

**工具集**：
为授权目的明确选择并固化的一组工具，是角色与具体工具之间的权限边界。
_避免_：工具目录、MCP 服务

**角色**：
授予 Agent 的可复用权限集合，通过关联工具集决定 Agent 可以发现和调用哪些工具；一个 Agent 可以拥有多个角色，其允许权限取并集。
_避免_：用户组、工作空间、租户

**平台管理员**：
管理网关全部配置、业务服务、工具、Agent、角色和发布操作的控制面用户。
_避免_：服务维护者、Agent

**审计查看者**：
只能查看配置、历史记录、调用审计和告警，不能修改系统状态的控制面用户。
_避免_：平台管理员、业务用户

**工具名称**：
工具在统一工具目录中的稳定全局标识，采用 `<服务标识>.<工具标识>` 的命名形式。
_避免_：显示名称、随机 ID

**服务实例**：
一个业务服务当前可接收请求的运行节点，由 Nacos 注册并被 MCP 网关发现。
_避免_：业务服务、工具

**OpenAPI 文档**：
描述业务服务 HTTP API 的机器可读契约，可从指定业务服务获取或由管理者提供，并用于生成工具定义。
_避免_：Swagger 文档
