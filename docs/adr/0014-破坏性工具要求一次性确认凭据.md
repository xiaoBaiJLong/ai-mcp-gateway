---
status: superseded by ADR-0019
---

# 破坏性工具要求一次性确认凭据

每次调用破坏性工具都必须携带由可信系统签发的一次性短期确认凭据，并绑定 Agent、最终用户、工具和参数摘要；写入工具可以单独配置是否要求确认。确认信息不能由模型作为普通参数声明，否则 Prompt Injection 可以自行伪造确认。该选择增加 Agent 宿主与网关的确认协议，但能在已有角色权限内阻止模型未经用户同意执行高风险操作。
