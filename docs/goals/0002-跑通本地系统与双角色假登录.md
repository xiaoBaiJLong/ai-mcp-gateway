# Goal：Issue #2 跑通本地系统与双角色假登录

> 来源：[GitHub Issue #2](https://github.com/xiaoBaiJLong/ai-mcp-gateway/issues/2)
> 生成基线：`main@87257483071514516913c4b566eba5aad80cc9a6`
> 当前状态：唯一前沿，可执行
> 阻塞：无

## Goal（目标）

建立可在本地运行的 Vue 3 前端、Spring Boot 后端和 MySQL/Redis/Nacos 依赖，并通过固定账号验证平台管理员可维护、审计查看者只读的角色边界。

## Current state（当前状态）

- 生成时仓库只有文档，不存在前后端源码、构建脚本、数据库迁移或测试。
- Ticket 的四条功能验收和两类测试均可证实未实现。
- 已确认技术栈、双角色固定账号、本地单实例和 Flyway 要求；不存在待用户决定的产品问题。
- 执行开始时必须重新记录分支、HEAD、工作区和新增验证命令；若 HEAD 已变化，以执行时 HEAD 为代码审查固定点。

## Execution order（执行顺序）

1. 建立最小前后端工程与清晰的管理模块/运行时模块边界，不引入 ticket 外功能。
2. 建立 MySQL 数据库基线和本地 MySQL、Redis、Nacos 依赖，提供健康状态。
3. 先写登录成功、失败和越权测试，再实现 `admin/666666`、`auditor/666666` 及最小受保护读写接口。
4. 让 Vue 薄客户端调用同一 REST API，完成登录和角色边界冒烟流程。
5. 补充中文本地启动与验证说明，从干净状态执行全部最小验证。

## Completion criteria（完成标准）

- [ ] 中文启动说明给出的命令可从干净环境启动 MySQL、Redis、Nacos、后端和前端。
- [ ] `admin/666666` 与 `auditor/666666` 均可通过公开登录接口登录，错误凭证被拒绝。
- [ ] 管理员能通过公开管理 REST API 完成至少一个受保护写操作。
- [ ] 审计查看者能读取同一资源，但写操作收到明确的权限拒绝且状态不改变。
- [ ] Flyway 数据库迁移可在空库执行，并能在已迁移数据库上再次启动而不报错。
- [ ] 健康检查能分别反映应用及核心依赖状态。
- [ ] 后端集成测试覆盖两种登录成功、错误凭证和审计查看者越权。
- [ ] 前端冒烟测试覆盖登录、管理员写入入口和审计查看者只读状态。
- [ ] 仓库中实际建立的前后端最小测试、类型检查和构建命令全部退出码为 0，并已写入中文文档。
- [ ] 已以执行开始时 HEAD 为固定点完成标准与规格双轴代码审查，且无未解决的高严重度问题。
- [ ] 全部标准通过后创建一个聚焦本 ticket 的本地提交，提交后工作区无无关修改。

## Constraints（限制）

- 不实现真实身份系统、多租户、多工作空间、生产高可用或 Kubernetes 交付。
- 假登录只用于第一阶段本地角色验证，不包装成可扩展认证平台。
- 使用 Java 21、Spring Boot 3.x、Vue 3、TypeScript、MySQL、Redis、Nacos 和 Flyway；不额外引入中间件。
- 前端只调用公开 REST API，不建立独立的后端业务逻辑。
- 保持单个后端应用，通过模块职责隔离管理与 MCP 运行时，不拆分服务。
- 不修改无关文件；默认不 push、不关闭 Issue、不修改 tracker。

## Context（上下文）

- 父规格：GitHub #1；当前 ticket：GitHub #2，评论为空。
- 必读：`AGENTS.md`、`CONTEXT.md`、`docs/requirements/mcp-gateway-requirements.md`。
- 相关 ADR：ADR-0001、ADR-0017、ADR-0018。
- 测试接缝：公开登录/管理 REST API及 Vue 薄客户端；依赖使用本地可控实例。
- 首先检查：当前 Git 状态、现有构建文件、测试脚本和 README 中的启动命令。

## Session recommendation（会话建议）

- 会话：新的单 ticket 目标会话。
- 能力级别：Standard。
- 推理强度：Medium。
- 理由：这是清晰的绿地工程骨架和角色冒烟功能，涉及多文件与集成测试，但产品边界已经明确。
