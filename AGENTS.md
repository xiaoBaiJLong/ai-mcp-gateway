## 智能体技能

### 事项跟踪

需求事项与规格存放在 `xiaoBaiJLong/ai-mcp-gateway` 的 GitHub Issues 中。参见 `docs/agents/issue-tracker.md`。

### 分诊标签

仓库使用五个默认分诊标签。参见 `docs/agents/triage-labels.md`。

### 领域文档

本仓库采用单一上下文的领域文档布局。参见 `docs/agents/domain.md`。

### Git 工作流

分支、提交与推送约定见 `docs/agents/git-workflow.md`。

### 开发完成

每次开发完成并完成相应验证后，必须将变更提交到当前 Git 分支，并推送到 GitHub 远程仓库。
